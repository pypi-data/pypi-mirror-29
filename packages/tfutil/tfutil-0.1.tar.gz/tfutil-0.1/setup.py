import setuptools

if __name__ == '__main__':
    setuptools.setup(
        name='tfutil',
        version='0.1',
        #packages=setuptools.find_packages(exclude=['tests', 'docs']),
        packages=['tfutil']
    )
