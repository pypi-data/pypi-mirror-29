# -*- coding: utf-8 -*-
# Manually written
from __future__ import unicode_literals

from django.db import migrations, models

import logging

logger = logging.getLogger(__name__)


def delete_timeentries_marked_for_delete(apps, schema_editor):
    TimeEntry = apps.get_model('orchestra', 'TimeEntry')

    for time_entry TimeEntry.objects.raw(
            'SELECT * FROM orchestra__timeentry where is_deleted=true'):
        print(time_entry)


class Migration(migrations.Migration):

    dependencies = [
        ('orchestra', '0044_auto_20160426_0044'),
    ]

    operations = [
        migrations.RunPython(delete_timeentries_marked_for_delete),  # manually-reviewed
    ]
