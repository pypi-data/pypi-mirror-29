from setuptools import setup
setup(
  name = 'niltechArduino',
  packages = ['niltechArduino'], # this must be the same as the name above
  version = '0.2',
  description = 'A lib to automatically detect and give usb port which is connected to arduino',
  author = 'Vaibhav VK - Niltech',
  author_email = 'vaibhav@niltech.in',
  #url = 'https://github.com/peterldowns/mypackage', # use the URL to the github repo
  #download_url = 'https://github.com/peterldowns/mypackage/archive/0.1.tar.gz', # I'll explain this in a second
  keywords = ['arduinoPort'], # arbitrary keywords
  classifiers = [],
)
