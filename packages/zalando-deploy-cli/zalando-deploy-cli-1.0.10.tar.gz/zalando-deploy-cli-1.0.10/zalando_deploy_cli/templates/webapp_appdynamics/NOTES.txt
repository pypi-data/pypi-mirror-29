Please change the following files according to your needs:

* pipeline.yaml: set target AWS account and cluster ID
* deployment.yaml: change Docker image, APPDYNAMICS_AGENT_APPLICATION_NAME inside the initContainer and environment variables
* apply/ingress.yaml: modify the DNS name
