from setuptools import setup, find_packages

setup(
    name='skytap-fixtures',
    version='1.0.7',
    description='A library to glue functions that produce test fixtures to the tests that use them.',
    author='Skytap, Inc.',
    packages=find_packages()
)
