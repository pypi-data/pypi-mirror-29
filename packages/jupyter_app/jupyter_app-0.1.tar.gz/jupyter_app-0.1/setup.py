#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

install_requires = \
['jupyterlab', 'webruntime', 'dialite']

entry_points = \
{'console_scripts': ['jupyter_app = jupyter_app:main']}

setup(name='jupyter_app',
      version='0.1',
      description='jupyter_app - run Jupyter lab as a desktop app',
      author='Almar Klein',
      author_email='almar.klein@gmail.com',
      url='https://github.com/almarklein/jupyter_app',
      py_modules=['jupyter_app'],
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.4',
     )
