#################
# Main Formulas 
#################
pipe_base_dir = ('{drive}', )
pipe_pr_dir = ('{pipe_base_dir}', 'projects')
pipe_mdl_dir = ('{pipe_base_dir}', 'mdl')
pipe_sbs_dir = ('{pipe_base_dir}', 'substance')
pipe_mtlx_dir = ('{pipe_base_dir}', 'mtlx')

pipe_data_dir = ('{pipe_base_dir}', '.data')
pipe_config_dir = ('{pipe_data_dir}', '.config')

pipe_cfg = ('{pipe_base_dir}', '.pipeline_config')


