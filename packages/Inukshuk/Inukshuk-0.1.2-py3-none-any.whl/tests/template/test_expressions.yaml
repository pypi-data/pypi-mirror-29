---
in: "{{foo[2]}}"
data:
    foo: ['one', 'two', 'three']
out: 'three'
---
in: "{{foo[1:]|join(', ')}}"
data:
    foo: ['one', 'two', 'three']
out: 'two, three'
---
in: "{{foo[1:4]|join(', ')}}"
data:
    foo: ['one', 'two', 'three', 'four', 'five']
out: 'two, three, four'
---
in: "{{foo[:3]|join(', ')}}"
data:
    foo: ['one', 'two', 'three', 'four', 'five']
out: 'one, two, three'
