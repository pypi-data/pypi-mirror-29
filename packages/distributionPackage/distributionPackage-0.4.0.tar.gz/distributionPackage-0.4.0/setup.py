#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['distributionPackage',
 'distributionPackage.app',
 'distributionPackage.app.tests',
 'distributionPackage.package',
 'distributionPackage.package.tests']

package_data = \
{'': ['*']}

install_requires = \
['packageManifest']

entry_points = \
{'console_scripts': ['makePackage = distributionPackage:main']}

setup(name='distributionPackage',
      version='0.4.0',
      description='A packaging utility for creating packages of files for distribution based on a YAML manifest.',
      author='Russell Smiley',
      author_email='im.russell.smiley@gmail.com',
      url='https://gitlab.com/blueskyjunkie/distribution-package',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3',
     )
