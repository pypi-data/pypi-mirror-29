#!/bin/bash

dir=/home/kortas/TSAR3D/maestro/pp

cd $dir
echo execution sur
cat $PBS_NODEFILE
cat $PBS_NODEFILE > machines.txt 


\rm OUT.*

python avanti_callback.py "hostname; date >> $dir/OUT.%d" 30 16
#dsh -f $PBS_NODEFILE "ps -edf | grep ^kortas"
