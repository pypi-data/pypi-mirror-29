
class MagicButton:
    '''
        def createObjects(self):
            self.joystick1.button3.whenPressed(self.foo.bar)
            
        def setupButtons(self, joystick):
            joystick[0].button[1].whenPressed(self.arm.shoot)
            
            joystick[0].button[1].whenPressed(self.foo.bar)
            
    '''
    