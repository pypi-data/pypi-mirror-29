Setting Text ForeGround Color. This can be used with Print Statement.

Eg:

Python3.x:

import bcolors as b
print(b.OK + "Color Statement" + b.END)


Python2.x:

import bcolors as b
print b.OK + "Color Statement" + b.END